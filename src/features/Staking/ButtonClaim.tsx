import { Button } from "@mantine/core";
import React from "react";

export const ButtonClaim = () => {
  return (
    <Button fullWidth variant="filled" color="green" tt="uppercase" disabled>
      Claim
    </Button>
  );
};
