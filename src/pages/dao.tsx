import { type NextPage } from "next";
import { Page, Section } from "~/components";

const Dao: NextPage = () => {
  return (
    <Page>
      <Section size={1000} mih="54vh">
        DAO
      </Section>
    </Page>
  );
};

export default Dao;
