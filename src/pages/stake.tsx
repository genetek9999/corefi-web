import { type NextPage } from "next";
import { Page, Section } from "~/components";

const Stake: NextPage = () => {
  return (
    <Page visibleGalaxy={false}>
      <Section size={1450}>alo</Section>
    </Page>
  );
};

export default Stake;
